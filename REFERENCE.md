# semantic-mask 레퍼런스

모든 수치는 **RTX 3080 Ti 12GB · 7776×3888 ERP 875장 · 대상=하늘**에서 실측했다.
다른 대상·해상도에서는 방향만 참고하고 값은 다시 재라.

---

## 1. 대상별 정책표

하늘에서 얻은 규칙 중 **둘은 다른 대상에서 뒤집힌다.** 이것이 프리셋을 나눈 이유다.

| 대상 | 클래스 | 타일 | 섬 제거 | 검사 | 주의 |
|---|---|---|---|---|---|
| `sky` | 2 | 4×2 | **켬** 100,000px | 천정·이음새 | 가장 검증됨 |
| `water` | 21,26,60,128,109,104 | 4×2 | 끔 | 이음새 | 반사·윤슬에서 흔들림, 하늘과 혼동 |
| `people` | 12 | **8×4** | 끔 | — | 작은 객체, 팽창 마진 권장 |
| `vehicles` | 20,80,83,102,116,127 | **8×4** | 끔 | — | 작은 객체 |
| `movers` | 위 둘 합 | **8×4** | 끔 | — | SfM/3DGS 이동체 제거 표준 |
| `vegetation` | 4,9,17 | 4×2 | 끔 | — | 잎 경계 반투명, 정련 이득 작음 |
| `ground` | 6,13,46,29,52,54 | 4×2 | 끔 | — | 지면 위 물체가 구멍으로 남는 게 정상 |
| `building` | 1,25,48 | 4×2 | 끔 | — | 유리 커튼월에서 흔들림 |

**섬 제거가 하늘에서만 성립하는 이유**: 항공 ERP 에서 지상 구조물은 전부 스카이라인을
통해 이어지므로 하늘 한복판에 떠 있을 것이 원리적으로 없다 — 태양·글레어·새뿐이고
전부 하늘로 봐야 맞다. 실측 139프레임에서 태양(최대 39,365px) 제거.
**사람·차·배는 그 자체가 고립된 섬**이므로 같은 정책을 쓰면 대상을 지운다.

**타일 크기가 역전되는 이유**: 넓은 영역은 맥락이 필요해 잘게 쪼개면 무너진다(아래 3-2).
작은 객체는 반대로 해상도가 필요하다. ※ 역전은 논리적 귀결이며 작은 객체로는 미검증.

---

## 2. 모델 선택 — 실패는 서로 겹치지 않는다

| 모델 | 매끈한 흰 구조물 | 엷은 구름 | 천정 | 3px 이내 | 시간 |
|---|---|---|---|---|---|
| SegFormer-B4 | **실패** | 정상 | 정상 | 56.0% | 2.0s |
| SegFormer-B5 | 실패 | 정상 | 정상 | B4와 동일 | 12.1s |
| Mask2Former Swin-L | 정상 | **실패 315/875장** | **실패 최대 22%** | 69.5% | 6.0s |
| **OneFormer Swin-L** | 정상 | 정상 | **0.000%** | 68.4% | 3.5s |
| SAM3 @2048 | — | — | **실패 96~100%** | 1.9% | 2.6s |
| PanoSAM 전천구 | — | — | 실패 | 19.7% | 25s |

**OneFormer 가 기본값**이다. 두 실패를 동시에 없앤 유일한 단일 모델이고 Mask2Former보다 빠르다.

**SAM 계열은 구조적으로 못 한다.** 인스턴스 분할이라 화면을 가득 채운 대상(천정의 하늘)을
물체로 못 잡고, 이진 마스크만 돌려줘 "점수를 확대한 뒤 문턱" 기법을 못 쓴다.
PanoSAM 처럼 투영을 바꿔 감싸도 이 약점은 따라온다.

---

## 3. 막힌 길 — 다시 파지 마라

### 3-1. 입력 해상도를 올리면 나빠진다 (VRAM 문제가 아니다)

| 입력 | 로짓 | 원본 px/칸 | VRAM | 3px 이내 | 구멍 | 대상 면적 |
|---|---|---|---|---|---|---|
| **640** | 192 | 10.1 | 1.8G | **67.9%** | 0 | 42.3% |
| 960 | 288 | 6.8 | 2.6G | 55.0% | 37,722 | 42.2% |
| 1280 | 384 | 5.1 | 3.8G | 52.1% | 1,699,426 | 36.7% |
| 1600 | 480 | 4.0 | 5.4G | 43.6% | 3,449,339 | 30.9% |

판정 칸이 2.5배 촘촘해졌는데 품질은 무너진다. **1600에서도 6.9GB가 남는다** —
메모리가 아니라 학습 분포를 벗어난 것이다. OOM 이 아니라 **조용한 정확도 하락**으로 나타난다.

### 3-2. 타일을 잘게 쪼개도 무너진다 (넓은 대상 한정)

| 타일 | 시간 | 3px 이내 | 대상 면적 |
|---|---|---|---|
| **4×2** | 3.5s | **67.9%** | 42.3% |
| 8×4 | 7.4s | 46.8% | **32.0%** |
| 12×6 | 7.6s | 44.7% | **28.4%** |
| 16×8 | 12.2s | 52.0% | **30.2%** |

SegFormer·Mask2Former·OneFormer **세 모델 모두**에서 재현. 하늘만 가득한 타일에는 판단 근거가 없다.

### 3-3. 정련 L 은 손잡이가 아니라 절벽

| L | 시간 | 카드 최대 | 3px 이내 |
|---|---|---|---|
| **900** | 12.5s | ~7,000MiB | 97.7% |
| 1000 | 13.1s | 9,260MiB | 97.7% |
| 1200 | 13.7s | 11,580MiB | 97.4% |
| 1400 | **94.6s** | **11,968MiB** | — |
| 1800 | **290.2s** | 13,000MiB | — |

절벽이 11,580 과 11,968MiB 사이에 있다. 품질은 그 구간에서 평평하다.

### 3-4. 기각된 정련 기법

| 기법 | 결과 | 이유 |
|---|---|---|
| SegRefiner (확산) | 86.3% / 76.2% | 균질한 흰 면을 노이즈로 보고 구멍을 뚫는다 |
| ViTMatte (매팅) | 81.2% / 66.0% | 하늘·건물 경계는 불투명해 매팅 전제가 성립 안 함 |
| CascadePSP+ViTMatte | 96.6% (악화) | 이미 제자리인 경계를 흔든다 |
| Depth Anything V2 | 6개 중 4개 악화 | 질감선을 따라다닌다 |
| 가이디드 필터 | 2.80→2.62px | 남은 오차가 흐림이 아니라 의미 판단 |
| 반복 정련 2~3회 | 1회에서 수렴 | — |

**CascadePSP 만 이긴 이유**: 다른 기법들은 마스크를 다시 그리려 든다.
CascadePSP 는 "어디가 대상인가"를 이미 정해진 것으로 받고 **경계선만 재배치한다.**

---

## 4. 측정 함정

- **`torch.cuda.max_memory_allocated()` 는 실사용을 3GB 가까이 과소보고한다.**
  텐서에 담긴 양만 세고 예약 블록·CUDA 컨텍스트·cuDNN 버퍼를 뺀다. `nvidia-smi` 로 재라.
- **같은 프로세스에서 여러 설정을 연속으로 재지 마라.** 캐싱 할당자 파편으로 135s vs 94.6s.
- **프레임당 시간이 5배까지 튄다**(12s~64s). 경계가 길수록 정련 창이 많다. 평균만 믿지 마라.
- **주 지표는 어떤 에지인지 모른다.** Depth 계열이 75% 를 찍고도 엉뚱한 질감선을 따라간 전례.
- **지표가 못 보는 결함이 있다.** 유리 타워를 폭 80px 파먹은 결함은 경계선 자체가
  멀쩡해 순위에 안 잡혔다. 최악 12장 분석에 그 유형이 하나도 없었다.
- **축소 검사는 이음새를 과대평가한다.** `verify.py` 는 속도 때문에 1/2 축소로 재는데
  최근접 보간이 경계 화소를 거칠게 잡아 원본 0.037% 가 0.162% 로 나온다.
  유무·순위 판정에는 무해하나 절대값 비교는 `--full-res` 로 하라.

---

## 4b. 보고에 반드시 들어갈 세 가지

절대경로 · 용량 · 예상시간. 셋이 빠진 보고는 다음 세션에서 쓸 수 없다.

| 항목 | 어디서 나오나 | 왜 |
|---|---|---|
| **절대경로** | `common.banner()` — 모든 스크립트 첫 줄 | 상대경로는 cwd 가 바뀌면 무의미. 어느 폴더에 썼는지가 가장 흔한 사고 |
| **용량** | `banner` 의 현재 용량, `plan_report` 의 예상 용량, `Progress.finish` 의 산출 용량 | 4단계 중간 산출물을 다 남기므로 디스크가 먼저 찬다. 다 돌고 알면 늦다 |
| **예상시간** | `analyze.py` 착수 전 추정, `Progress.tick` 의 남은시간·완료예정시각 | 정련은 시간 단위다. 착수 전에 알아야 승인이 된다 |

비용 상수는 `common.STAGE_SEC`(프레임당 초 @30.2MP), 용량 상수는 `common.MASK_BYTES_PER_MP`
(하늘 실측 1.5KB/MP, 안전하게 2KB 로 잡음). 대상이 잘게 흩어지면 PNG 가 커진다.

`plan_report` 는 여유 공간이 예상 용량의 1.2배 미만이면 경고한다.

---

## 5. 알려진 실패 유형 (미해결)

| 유형 | 증상 | 대응 |
|---|---|---|
| 유리 커튼월 | 하늘 반사를 하늘로 봐서 정련이 타워를 세로로 파먹음 | **마스크 연산 불가.** 성긴 마스크를 넉넉본으로 별도 산출 |
| 흐린 원경 | 대기 원근으로 대비 소실, 스카이라인이 뭉개짐 | 정련이 75→85% 회복. 잔여는 한계 |
| 과노출 태양 | 하늘로 판정 안 됨 | **섬 제거로 해결** (`--speck-island 100000`) |

**과확장 제한이 유리 커튼월에 실패하는 이유**: 쐐기는 깊은 덩어리가 아니라
폭 40~80px·길이 1000px 의 얇고 긴 띠다. 정당한 보정과 둘 다 경계에 붙어 있어
깊이로 구분되지 않는다. 문턱을 낮추면 문제 프레임을 포함해 전부 나빠진다.

| 과확장 제한 | 문제 프레임 | 정상 A | 정상 B |
|---|---|---|---|
| 없음 | 96.7% | 97.7% | 82.7% |
| 40px | 96.5% | 97.5% | 82.3% |
| 5px | 89.2% | 91.6% | 75.4% |

---

## 6. ERP 이음새

타일링에는 `wrap` 을 넣었는데 정련 단계에 같은 고려를 안 하면 깨진다.

| 단계 | 평균 | 최대 | 1% 초과 |
|---|---|---|---|
| 패스1 (wrap 타일링) | 0.061% | — | 14장 |
| 정련 직후 | **1.076%** | **13.04%** | **236장** |
| 이음새 복구 후 | 0.037% | 2.49% | 5장 |

복구는 이음새를 중앙에 오도록 감고 좁은 띠만 재정련해 중앙부만 붙인다.
띠 가장자리에서 500px 안쪽에 붙여 두 정련본이 같은 국소 맥락을 보게 한다 —
붙인 경계의 불연속이 임의의 열과 같은 수준(0.00~0.18% vs 0.03~0.08%)이다.

---

## 7. 최종 파이프라인과 결과 (하늘, ERP 875장)

```
패스1  OneFormer 4×2 wrap              15분
패스2  CascadePSP L=900                 3시간   ← 단일 워커가 빠르다
패스3  이음새 복구                        64분   ← ERP 전용
패스4  후처리 (과확장 5px·구멍 2,000·섬 100,000)  15분
```

| 항목 | 값 |
|---|---|
| 경계 3px 이내 | 평균 95.2% · 중앙값 95.7% |
| 천정 누락 | 0.000% |
| 이음새 불일치 | 평균 0.037% |
| 태양 | 139프레임 전부 제거 |
| 마스크 용량 | 장당 45KB, 단계당 약 40MB, 4단계 전체 약 160MB |

산출물은 두 벌을 권한다 — **본판**(경계 정밀도 우선)과 **넉넉본**(성긴 마스크 그대로,
구조물 보존 우선). 정련이 만드는 실패가 성긴 마스크엔 없고, 틀리는 방향이 일관되게 안전하다.
